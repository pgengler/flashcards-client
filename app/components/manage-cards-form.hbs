<form id="manage-cards" {{on "submit" (prevent-default this.saveCards)}} data-test-manage-cards-form>
  <table class="table">
    <tbody>
      {{#each this.listItems as |item|}}
        <tr data-test-card>
          <td>
            <input type="checkbox" value={{item.card.id}} checked={{item.checked}} />
          </td>
          <td>
            {{item.card.front}}
          </td>
          <td>
            {{item.card.back}}
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  <button type="submit" class="btn btn-primary">
    Save
  </button>

  <button
    type="button" class="btn btn-outline-primary"
    data-test-cancel-button
    {{on "click" @close}}
  >
    Cancel
  </button>
</form>
